# MacOS Pipeline for SoundBox

trigger:
- master
- azure-pipeline-cmake

pool:
  vmImage: 'macOS-latest'
steps:
- script: |
        echo hello from macOS
        cmake --version
        python3 --version
        mkdir build
        cd build
        cmake ../source
        cmake --build .
